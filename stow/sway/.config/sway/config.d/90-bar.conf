# Status Bar: waybar
#
# Read `man 5 sway-bar` for more information about this section.
# Read `man 5 waybar` for more information about the waybar instance style and layout configuration
#
# Requires: waybar
#
# Ported verbatim from Fedora's /usr/share/sway/config.d/90-bar.conf.
# This is the fragment that actually starts waybar -- nothing else does.

bar {
    swaybar_command waybar
}
