# ssh-agent keys to load. One key path per line. '~' expands to $HOME. '#' starts a comment.
# Every SSH key use will be logged via SSH_ASKPASS (audit.log).
# Re-run `ssh-agent-load-keys` after editing.

~/.ssh/id_ed25519
~/.ssh/id_ed25519_recombee_sk
~/.ssh/id_zezav_ed25519
~/.ssh/id_ed25519_cdn-deploy
~/.ssh/id_rsa
~/.ssh/id_ed25519_isee

# Uncomment the ones you want pre-loaded at login:
