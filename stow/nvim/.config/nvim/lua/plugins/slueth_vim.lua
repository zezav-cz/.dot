-- Indentation auto-detection (vim-sleuth)
-- Automatically adjusts workspace `shiftwidth` and `expandtab` settings
-- heuristically based on the existing indentation style of the current file.
return { "tpope/vim-sleuth" }
